
Ettercap now has unit-tests!

Requires:
  check (will use bundled check if it isn't already installed)

- To enable tests:
  - mkdir build 
  - cd build
  - cmake -DENABLE_TESTS=On ..
  - make && make test
    OR
  - make && make test_verbose

