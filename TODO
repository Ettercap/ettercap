==================
  TODO for NG
==================

>>> things to be fixed
   
   + FIXING
      - dissect_free_session
      - what about truncated pcap file ?
      - input for curses gui
      - in unoffensive we get UNKNOWN for all the profiles

   + PCAP RELATED
      + when libpcap 0.8.1 will be released
         - pcap_datalink_val_to_name  
         - pcap_lib_version
      + when libpcap 0.8.x will be released
         - pcap_stat fix (even under linux the stats are cumulative)

   + BSD PORTING
      - find a way for bridged sniff under OpenBSD 

   + SOLARIS PORTING
      - test the bridged sniffing

   + WINDOWS PORTING
      - get_iface_mtu 
      - make the whole thing compile ;)
      - then found all the bugs....
      - dll instead of plugins (argh!)

>>> to be developed...

this is the unofficial and floating in time roadmap:

   + DOCUMENTATION
      - write the requirements for each os
      - write a man for the curses GUI
      - developer docs
         - plugins

   + LIBWDG
      - logging menu
      - fix the box() under freebsd
      - widgets:
         - selectable lists (auto refresh)
         - compound objects

   + INJECTION 
      - it is done by the ui looking the sessions (locking them)
      - reset modified connection (on user request)
      - flag to mark encripted connections
      - pptp
      - gre

   + DISSECTORS
      - convert all the old dissectors:
         - https (over the ssl one)
         - ldap
         - mountd
         - pcanywhere
         - yahoo
      - SSL unified dissector for https, impas, pop3s ecc ecc
      - TDS
   
   + LOGGING
      - selective log on connections
      
   + PLUGINS
      - automatic add of new hosts
      port all the plugins to NG:
         - lurker    -> find_ettercap
         - troll     -> arp_responder
         - roper     -> stop_ipsec
         - dwarf     -> (will be integrated with etterlog)
         - thief     -> (same as dwarf)
         - arpcop    -> arpcop
         - banshee   -> killer
         - golem     -> res_exhaust
         - imp       -> nbt_names
         - lamia     -> stp_root (or mitm attack?)
         - leech     -> isolate
         - ooze      -> will not be ported (never used)
         - shadow    -> will not be ported (never used) (use nmap instead)
         - triton    -> gw_discover

   + BRIDGED SNIFFING
     
   + CONNECTION
      - kill a connection
      - type (service)
     
   + BINDER

   + ETTERLOG
      - extract files from streams looking for mime content
   
   + FILTERING ENGINE
      - quick filter on a connection


2004:
   
   + INTERFACE ENGINE
      - gtk

   + WIFI decoder support for WEP (key given)

==================
  LONG TERM TODO
==================

   + SOCKS
      - decode the internal connection

   + multiple interfaces on gateway

   + NEW DISSECTOR
      - TACACS dissector
      - Microsoft RPC
      - postgresql

   + Inet
      - handle fragmented packets
      - IPv6 support


