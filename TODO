==================
  TODO for NG
==================

>>> things to be fixed

   + lnet ptag thread safe (check it)

   !! dissector malloc the DISSECTOR, someone have to free them... !!

   + PLUGINS ENGINE
      - replace the search point form . to INSTALL_PREFIX/lib
      - cygwin issue (study libltdl)
      - use libldtdl (problems even on openbsd)
      - LTDL_SHLIB_EXT
      - make test with dlopen-test

   + SOLARIS PORTING
      - checksum (WORD_BIGENDIAN on intel ???)

   + BSD PORTING
      - hack_pcap_lnet is not usable
      - bpf filter on outgoing packets
      - BIOCSSEESENT (only under freebsd)

>>> current developmet state 

   + TOP HALF
      - passive profiling (almost done in etterlog)
      - connection tracking (with buffered conn)


   + ARP POISON ENGINE
      - arp poisoner
      - multipoison
      - rearp in demon mode (clean shutdown of all thread)
   
   + FILTERING ENGINE
      - use regexp for filtering
      - include the ip in the filter rule
      - filter even the header of the packets
      - use bison and flex
      - scripting language
      - etterfilter (utility)
   
   + INJECTION 
      - it is done by the ui looking the sessions (locking them)
      - binder
   
   + DISSECTORS
      - convert all the old dissectors
      - HTTPS without the dirty trick (can we use loopback?)
      - SSH has to support DES, 3DES, blowfish and zlib compression
      - WIFI decoder support for WEP (key given)
      
   + PLUGINS
      - convert all the old plugins
      - check for poisoner
      - linktype
      - "active" passive fingerprint
     
   + INTERFACE ENGINE
      - console
      - ncurses
      - gtk

   + DEVELOPMENT DOCUMENTATION

   + ETTERLOG
      - extract files from streams looking for mime content

==================
  LONG TERM TODO
==================

   + SFTP
   + FTP-SSL

   + NEW DISSECTOR
      - TACACS dissector
      - new netbios
      - postgresql
      - cvs
      - tds

   + Inet
      - handle fragmented packets
      - IPv6 support

   + Ncurses interface
      - better winch handling... (i'm too lazy to do this...)

   + PORTING TO 64 bit ARCH

=================
  TODO NEVER ;)    or very very very long term todo... :)
=================

   + GTK+ interface

   + Kettercap (KDE interface)


