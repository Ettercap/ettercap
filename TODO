==================
  TODO for NG
==================

>>> things to be fixed
   
   + FIXING
      - dissect_free_session
      - input for curses gui

   + PCAP RELATED
      + when libpcap 0.8.2 will be released
         - pcap_stat fix (even under linux the stats are cumulative)

   + LNET RELATED
      + when libnet 1.1.2 will be released
         - add support for unconfigured interface
         - check if the icmp redirect gw is correct

   + BSD PORTING
      - find a way for bridged sniff under OpenBSD 

   + SOLARIS PORTING
      - test the bridged sniffing

   + WINDOWS PORTING
      - get_iface_mtu 
      - make the whole thing compile ;)
      - then found all the bugs....
      - dll instead of plugins (argh!)

>>> to be developed...

this is the unofficial and floating in time roadmap:

   + DOCUMENTATION
      - write the requirements for each os
      - how to code a plugin 

   + CURSES GUI
      - profile management
      - connection management
      + LIBWDG
         - fix the box() under freebsd
         - mouse events on focus

   + INJECTION 
      - it is done by the ui looking the sessions (locking them)
      - reset modified connection (on user request)
      - flag to mark encripted connections
      - pptp
      - gre

   + DISSECTORS
      - convert all the old dissectors:
         - https (over the ssl one)
         - pcanywhere
         - yahoo
      - SSL unified dissector for https, impas, pop3s ecc ecc
      - TDS
   
   + LOGGING
      - selective log on connections
      
   + PLUGINS
      - automatic add of new hosts
      port all the plugins to NG:
         - lurker    -> find_ettercap
         - golem     -> res_exhaust
         - leech     -> isolate
         - triton    -> gw_discover

   + BRIDGED SNIFFING
     
   + CONNECTION
      - kill a connection
      - type (service)
    

============================
  Not in the first release  
============================
   
   + INTERFACE ENGINE
      - gtk

   + BINDER
   
   + ETTERLOG
      - extract files from streams looking for mime content or protocol
   
   + FILTERING ENGINE
      - quick filter on a connection

==================
  LONG TERM TODO
==================
   
   + WIFI decoder support for WEP (given the key)

   + SOCKS
      - decode the internal connection

   + multiple interfaces on gateway

   + NEW DISSECTOR
      - TACACS dissector
      - Microsoft RPC
      - postgresql

   + Inet
      - handle fragmented packets
      - IPv6 support


